Open your telegram app and create contact with  botfather
Create a new telegram bot by initializing the chat with botfather and type /newbot and name it
install telepot using pip install telepot command in cmd
Make our first API call getMe() which returns the  information of the our bot
getUpdates() gives the update of the bot
The code will return the msg you sent to the bot as you said "msg"
